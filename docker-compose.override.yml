version: '3.7'
services:
  colorwheel_client:
    depends_on:
      - mongodb
    ports:
      - '3000:3000'
  mongodb:
    ports:
      - '27017:27017'
    volumes:
      - mongo-data:/data/db
volumes:
  mongo-data: